%h1 Банк : Перечислить деньги

= form_tag transactions_path do

  = label :transaction, :to, 'Счет получателя'
  = text_field :transaction, 'to'

  = label :transaction, :sum, 'Сумма'
  = number_field :transaction, :sum

  = submit_tag 'Перечислить'
